---
description: "noteのMarkdown仕様と推奨フォーマットを定義するルール。"
alwaysApply: true
---

# Note特化フォーマット

- 見出しは `##` (h2) から始める。
- 画像には必ず代替テキスト (`alt`) を設定する。
- 強調は `**太字**` を使用する。
- noteの引用 `>` やコードブロック ``` の仕様に準拠する。

